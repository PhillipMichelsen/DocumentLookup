version: '3.8'

services:
  gateway_service_api:
    build: ./Gateway-Service
    ports:
      - "8000:8000"
    depends_on:
      - rabbitmq
      - redis

  task_orchestrator_service_api:
    build: ./Task-Orchestrator-Service
    depends_on:
      - rabbitmq
      - redis

  embedding_service_api:
    build: ./Embedding-Service
    depends_on:
      - rabbitmq

  file_service_api:
    build: ./File-Service
    depends_on:
      - rabbitmq
      - file_service_minio
      - file_service_postgres

  file_service_grobid:
    image: lfoppiano/grobid:0.7.2
    ports:
      - "8070:8070"

  file_service_minio:
    image: minio/minio
    command: server --console-address ":9001" --address ":9000" /data
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - file_service_minio_data:/data

  file_service_postgres:
    image: postgres
    ports:
      - "5432:5432"
    volumes:
      - file_service_postgres_data:/data
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin123
      - POSTGRES_DB=file_service


  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "15672:15672"
      - "5672:5672"
    environment:
      - RABBITMQ_DEFAULT_USER=admin
      - RABBITMQ_DEFAULT_PASS=admin123
    
  redis:
    image: redis
    ports:
      - "6379:6379"


volumes:
  file_service_minio_data:
  file_service_postgres_data:


